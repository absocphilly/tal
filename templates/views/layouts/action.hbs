{{>HtmlBegin}}
  {{>SiteHeader}}
  {{>PageHeaderWhite}}
  {{>BodyBegin}}
  	{{>MainBegin}}
      {{{body}}}
  	{{>MainEnd}}
  {{>BodyEnd}}
  {{>CallsToAction}}
  {{>StatsDark}}
  {{>SiteFooter}}
{{>HtmlEnd}}